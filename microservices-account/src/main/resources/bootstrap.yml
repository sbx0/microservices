spring:
  application:
    name: account
  cloud:
    inetutils:
      preferred-networks: 192.168.1\.(.*)
    config:
      uri: http://${CONFIG_HOST:192.168.1.254}:${CONFIG_PORT:8888}
      fail-fast: true